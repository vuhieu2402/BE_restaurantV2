Django>=4.2,<5.0
djangorestframework>=3.14.0
psycopg2-binary>=2.9.0
Pillow>=10.0.0
python-decouple>=3.8
django-storages>=1.14.0
boto3>=1.28.0
drf-spectacular>=0.27.0
django-filter>=23.0
django-cors-headers>=4.3.0
pyyaml>=6.0

# Celery dependencies for asynchronous task processing
celery[redis]>=5.3.0
redis>=4.5.0
django-redis>=5.4.0
flower>=2.0.0  # Optional: for Celery monitoring

# JWT tokens
djangorestframework-simplejwt>=5.3.0

# Email and SMS support
twilio>=8.10.0  # Optional: for SMS service integration

# Chatbot dependencies
openai>=1.0.0  # OpenAI-compatible client (for OpenRouter API)
groq>=0.9.0  # Groq API client (fastest inference)

# Django Channels and ASGI server for WebSocket support
channels>=4.0.0
channels-redis>=4.1.0
daphne>=4.0.0  # ASGI server for production WebSocket
